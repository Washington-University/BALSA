<div class="mt-2">
<g:radioGroup name="${category.name}" id="categoryOptions" labels="${category.options}" values="${category.options}" class="searchRefresh">
<div class="d-inline mr-4">${it.radio} ${it.label}</div>
</g:radioGroup>
</div>